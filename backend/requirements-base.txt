# Core application and runtime dependencies shared by every image flavor.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2
structlog==26.1.0
aiosqlite==0.22.1
aiofiles>=25.1.0
httpx==0.28.1
python-multipart==0.0.32

# Image processing
Pillow>=12.3.0
numpy<3.0.0
opencv-python-headless<4.11

# TensorFlow Lite compatibility. ARM64 uses Google's current standalone LiteRT
# interpreter rather than the much larger, retired tensorflow-aarch64 wheel.
# Non-Linux development keeps TensorFlow's native package for compatibility.
tensorflow-cpu; python_version >= '3.12' and sys_platform == "linux" and platform_machine != "aarch64" and platform_machine != "arm64"
ai-edge-litert==2.1.6; python_version >= '3.12' and sys_platform == "linux" and (platform_machine == "aarch64" or platform_machine == "arm64")
tensorflow; python_version >= '3.12' and sys_platform != "linux"

aiomqtt==2.5.1
alembic==1.19.0
prometheus-client
slowapi==0.1.10

# Authentication
pyjwt==2.13.0
cryptography==50.0.0
bcrypt==5.0.0

# SMTP and OAuth notifications
aiosmtplib==5.1.2
google-auth==2.56.3
google-auth-oauthlib==1.4.0
jinja2==3.1.6
