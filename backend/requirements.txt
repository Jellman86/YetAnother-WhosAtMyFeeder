fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.2.1
structlog==24.1.0
aiosqlite==0.19.0
aiofiles>=23.0.0
httpx==0.27.0
python-multipart==0.0.9
# Image processing
Pillow>=10.2.0
numpy<2.0.0
# Machine Learning
# Note: tflite-support wheels for 3.12 might be scarce, fallback to tflite-runtime or tf-cpu might be needed. 
# Starting with tflite-runtime if available or large tf-cpu.
# For now, let's try tflite-support and see if it installs, or use a specific known compatible version.
# tflite-support==0.4.4
tensorflow-cpu; python_version >= '3.12'
aiomqtt==2.0.1
opencv-python-headless>=4.9.0
pytest==8.0.0
pytest-asyncio==0.23.5
